// RUN: ! %verify "%s" >> "%t"
// RUN: %diff "%s.expect" "%t"

datatype Instr = Branch(m: nat)

predicate WellTyped(ii: seq<Instr>, typing: seq<nat>) {
  forall pc :: 0 <= pc < |ii| ==>
    match ii[pc]
    case Branch(n) =>
      typing[pc + n] == typing[pc]
}