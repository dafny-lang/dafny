// RUN: ! %verify %s &> "%t"
// RUN: %diff "%s.expect" "%t"

function ElseError(x: int): int
  ensures ElseErorr(x) == 0
{
  if x == 0 then
    0
  else 
    1
}
function ThenError(x: int): int
  ensures ThenError(x) == 0
{
  if x == 0 then
    0
  else 
    1
}

function CaseError(x: int): int
  ensures CaseError(x) == 1
{
  match x {
    case 0 => 0
    case 1 => 1
    case 2 => 2 
  }
}