The JavaScript runtime now compares references without an `equals` method by identity instead of crashing with "a.equals is not a function". Besides extern references, this fixes generic comparison — sets, maps, sequence membership, datatype fields, equality at a type parameter — of multi-dimensional arrays and of iterators, neither of which involves extern code; defining an `equals` method remains the opt-in for extern value types
