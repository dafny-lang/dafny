Fix a soundness bug: an extreme (`least`/`greatest`) predicate may not quantify over a type as large as the ordinals, but the check looked only at the bound variable's type arguments. It now also looks inside datatype and codatatype fields, and rejects ranging over all of a type parameter or abstract type, which could be instantiated with such a type. Quantification confined to a finite range is still allowed
